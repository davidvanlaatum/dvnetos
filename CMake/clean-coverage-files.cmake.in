foreach (DIR @COVERAGE_INPUT_FILES@)
  file(GLOB files ${DIR})
  if (files)
    foreach (file ${files})
      message(STATUS "deleting ${file}")
      file(REMOVE ${file})
    endforeach ()
  endif ()
endforeach ()
